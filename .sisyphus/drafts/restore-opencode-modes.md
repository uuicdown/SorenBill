# Draft: 保留 OpenCode 原生 Plan/Build 模式

## 已确认的需求
- 用户声明：安装 oh-my-openagent 插件后，原本可用的 OpenCode TUI 的 Plan/Build（按 Tab 切换）模式不再可用。用户需要保留原生的 Plan/Build 模式。

## 我已做的探索（证据）
- 读取：C:\\Users\\rony\\.config\\opencode\\opencode.json — 包含 "plugin": ["oh-my-openagent@latest"]（插件已启用）。
- 读取：C:\\Users\\rony\\.config\\opencode\\opencode.json.tmp — 存在可能被客户端读取的临时配置，也指向该插件。
- 读取：C:\\Users\\rony\\.config\\opencode\\oh-my-openagent.jsonc（插件配置），未发现显式的 "modes" / "keybind" / "planMode" / "buildMode" 配置。

## 初步技术判断（已决定）
- 配置层面没有发现可直接翻转的 JSON 开关 → 失去 Plan/Build 行为很可能由插件实现代码在运行时替换或拦截 TUI 键位（最可能）。
- 试验性验证（安全、可回退）是：临时禁用插件并重启 opencode，观察 Plan/Build 是否恢复。该步骤是必做的第一步，以便把插件本身作为“原因”或“排除项”。

## 开放问题（需要用户偏好）
- 您希望哪一种结果？（请在下面选项中选择）
  1) 立即禁用插件以恢复原生 Plan/Build（推荐：最快、最安全）。
  2) 保留插件但查找/修改插件（或其安装位置）以让它不干扰 Tab 切换（需要查看插件代码或作者支持，工作量中等）。
  3) 同时保留插件和原生体验：为不同场景使用不同配置/启动脚本（例如：保留默认配置但添加一个不加载插件的 profile/脚本来启动原生 opencode）。

## 范围边界
- 包含：检测/恢复 OpenCode 的 Plan/Build（Tab）切换行为；最小改动优先：先通过配置变更验证问题来源；如需修改插件代码，则只提供可复制的定位与变更建议（不在本步骤内修改源代码）。
- 排除：直接替换或修改 opencode 二进制；上游 plugin 仓库提交（可作为后续建议）。

## 下一步（针对不同偏好，我会生成逐步、可复制的操作清单）
- 如果您选择 1（推荐）：我将生成一套“备份并临时禁用插件 → 重启 opencode-cli → 验证 Plan/Build 恢复 → 恢复配置”的决策完整命令清单（Windows PowerShell 逐行、可直接粘贴执行）。
- 如果您选择 2：我将列出插件安装位置、需要检查的文件清单（搜索关键字：tab、keypress、tui、mode、plan、build），并生成补丁式定位步骤与可选修复（增加保留原生模式的配置入口或本地小修）。
- 如果您选择 3：我将生成两个并存的 profile/启动脚本（"opencode-native.ps1" 与 "opencode-withplugin.ps1"），并说明如何在不卸插件的情况下以原生模式启动。

## 备注
- 基于探索结果，我不会修改任何代码或配置，除非您明确授权。我将提供每一步的可回滚命令与恢复方法。

## 用户选择（已确认）
- 用户选择了方案 1：优先尝试“无代码”方案 —— 通过配置/启动脚本或已有插件选项恢复原生 Plan/Build；仅在配置不可行时，才考虑局部修改插件代码（最低侵入）。

## 当前行动项
- 我已在后台启动深入搜索（task id: bg_29544425），目标是找出可通过配置或启动参数解决的问题点（例如：opencode 启动加载顺序、临时/备份配置的优先级、插件是否提供可配置的“preserveTui”选项、或可用的启动开关）。
- 下一步：等待后台探查完成 → 基于探查结果生成逐行可复制的 PowerShell 验证与修复脚本（包含备份/回滚步骤）。
